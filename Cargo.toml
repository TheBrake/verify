[package]
name = "verify"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Ultra-fast local Git pre-push hook that blocks leaked secrets."
license = "MIT"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
panic = "abort"
